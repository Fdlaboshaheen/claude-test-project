Summarize the current git changes in plain English.

1. Run `git diff HEAD` to see all changes.
2. Run `git status` to see staged/unstaged files.
3. Write a short, non-technical summary of what changed and why it matters — as if explaining to a teammate in a standup.
